{
  "name": "di-container",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling (commit linting and git hooks) for the wptechnix/di-container PHP package.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.1.0"
  }
}
